<Window x:Class="Lib_System.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="250" Width="300">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Username:" Grid.Row="0" Margin="0,0,0,5"/>
        <TextBox x:Name="UsernameBox" Grid.Row="1" Height="25"/>
        <TextBlock Text="Password:" Grid.Row="2" Margin="0,10,0,5"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="3" Height="25"/>
        <StackPanel Orientation="Horizontal" Grid.Row="4" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Login" Width="80" Height="30" Click="Login_Click" Margin="0,0,10,0"/>
            <Button Content="Register" Width="80" Height="30" Click="Register_Click"/>
        </StackPanel>
    </Grid>
</Window>
