<Window x:Class="Lib_System.Views.LogAuditEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Edit Audit" Height="250" Width="350">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="User ID:"/>
        <TextBox x:Name="UserBox" Grid.Row="1" Margin="0,5"/>
        <TextBlock Text="Action:" Grid.Row="2" Margin="0,10,0,0"/>
        <TextBox x:Name="ActionBox" Grid.Row="3" Margin="0,5"/>
        <TextBlock Text="Created At (yyyy-MM-dd HH:mm:ss):" Grid.Row="4" Margin="0,10,0,0"/>
        <TextBox x:Name="CreatedBox" Grid.Row="5" Margin="0,5"/>
        <StackPanel Orientation="Horizontal" Grid.Row="6" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="OK" Width="80" Margin="0,0,10,0" Click="Ok_Click"/>
            <Button Content="Cancel" Width="80" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
