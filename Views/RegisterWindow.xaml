<Window x:Class="Lib_System.Views.RegisterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Register" Height="300" Width="350" WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Username:" Grid.Row="0" Margin="0,0,0,5"/>
        <TextBox x:Name="UsernameBox" Grid.Row="1" Height="25"/>
        <TextBlock Text="Email:"    Grid.Row="2" Margin="0,10,0,5"/>
        <TextBox x:Name="EmailBox"    Grid.Row="3" Height="25"/>
        <TextBlock Text="Password:" Grid.Row="4" Margin="0,10,0,5"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="5" Height="25"/>

        <StackPanel Orientation="Horizontal" Grid.Row="6" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Register" Width="80" Margin="0,0,10,0" Click="Register_Click"/>
            <Button Content="Cancel"   Width="80"             Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
