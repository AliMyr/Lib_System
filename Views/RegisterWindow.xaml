<Window x:Class="Lib_System.Views.RegisterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Register" Height="250" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Username:"/>
        <TextBox x:Name="UsernameBox" Grid.Row="1" Margin="0,5"/>

        <TextBlock Text="Email:" Grid.Row="2" Margin="0,10,0,0"/>
        <TextBox x:Name="EmailBox" Grid.Row="3" Margin="0,5"/>

        <TextBlock Text="Password:" Grid.Row="4" Margin="0,10,0,0"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="5" Margin="0,5"/>

        <StackPanel Orientation="Horizontal" Grid.Row="6" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Register" Width="80" Click="Register_Click"/>
            <Button Content="Cancel"   Width="80" Margin="10,0,0,0" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
